# url-shortner

Local env setup

- `git clone git@github.com:emrekap/url-shortner.git`
- `cd url-shortner`
- `npm i`
- `npm run envgen`
- `docker-compose up`
- `npx prisma migrate deploy` 
- `npm run start:dev`

---
